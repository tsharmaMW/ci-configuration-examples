version: '2.1'
setup: true
orbs:
  path-filtering: circleci/path-filtering@0.1.7
workflows:
  generate-config:
    jobs:
      - path-filtering/filter:
          # config-path: .circleci/ToolboxDistribution.yml
          mapping: |
          .* .circleci/ToolboxDistribution.yml
